# This is a simple application to demonstrate how Spring app services can work together with jaeger and elasticsearch.

## Build and Run
Execute in the root directory:
```
./mvnw clean package
docker-compose up --build
```
